import { component } from 'picoapp';
import LazySizes from "lazysizes";

export default component(({ node }) => {
    var lazySizesInstance = new LazySizes({
    // Custom settings go here
    });

});
